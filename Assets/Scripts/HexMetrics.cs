using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class HexMetrics {

    public static readonly float innerRadius = 0.5f;
    public static readonly float outerRadius = innerRadius * (2 / Mathf.Sqrt(3));

}
